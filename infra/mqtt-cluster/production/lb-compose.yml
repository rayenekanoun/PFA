name: mqtt-lb

services:
  haproxy:
    image: haproxy:2.9
    container_name: mqtt-lb
    restart: unless-stopped
    ports:
      - "${LB_MQTT_PORT:-1883}:1883"
      - "${LB_STATS_PORT:-8404}:8404"
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
