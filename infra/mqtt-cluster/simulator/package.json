{
  "name": "mqtt-diagnostic-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic MQTT diagnostic simulator for EMQX cluster validation.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:contract": "vitest run tests/contract.test.ts"
  },
  "dependencies": {
    "mqtt": "^5.14.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.17.57",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
